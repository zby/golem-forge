/**
 * Worker File Parser
 *
 * Re-exports from @golem-forge/core for backwards compatibility.
 * The canonical implementation is now in the core package.
 *
 * @module worker/parser
 */

// Re-export from core
export { parseWorkerString } from '@golem-forge/core';
export { formatParseError } from './schema.js';
