/**
 * Toolset Registry (Re-exported from Core)
 *
 * The registry is now in @golem-forge/core.
 * This file re-exports for backwards compatibility with existing CLI imports.
 */

export {
  ToolsetRegistry,
  type NamedTool,
  type Toolset,
  type ToolsetContext,
  type ToolsetFactory,
  type ExecutionMode,
  type ManualExecutionConfig,
} from "@golem-forge/core";
